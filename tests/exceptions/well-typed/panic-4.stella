language core;

extend with
  #comparison-operators,
  #arithmetic-operators,
  #panic;

fn div(n : Nat) -> fn(Nat) -> Nat {
  return fn(m : Nat) {
    return panic!
  };
}

fn main(n : Nat) -> Nat {
  return div(n)(0);
}
